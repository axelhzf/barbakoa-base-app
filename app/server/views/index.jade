extends layout

block head
  link(rel="stylesheet", href="/assets/" + assets.style)

block content
  h1 Barbakoa
  h2 Total #{visits.length}

  each visit in visits
    div #{visit.agent}

  if assets.js
    each script in assets.js
      script(src="assets/" + script)